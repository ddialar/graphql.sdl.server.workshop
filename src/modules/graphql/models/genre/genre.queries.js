import * as GenresServices from '../../../../../src/core/services/genres.services';

// ###############################################################
// ##########            READING OPERATIONS             ##########
// ###############################################################

// 1. Define the 'getAllGenres' method.
const getAllGenres = async (parentValues, args, context, astData) => {
    try {
        // 1.1. Return the result of the 'getGenresData' method contained into the 'GenresServices' services. Pay attention because this method is asynchronous.
    } catch (error) {
        throw error;
    }
};

// 2. Define the 'Queries' const as object, including the 'getAllGenres' method within it.

export {
    // 3. Export an object that contains the 'Queries' constant.
};