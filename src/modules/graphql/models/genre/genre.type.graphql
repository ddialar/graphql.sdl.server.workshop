# import MovieType from '../movie/movie.type.graphql'

type GenreType {
    id: Int
    name: String
    movies: [MovieType]
}

type Query {
    getAllGenres: [GenreType]
    getGenreById(
        id: Int!): GenreType
}