# # import MovieType from '../movie/movie.type.graphql'

type ActorType {
    id: Int
    name: String
    """Actor/Actress' picture URL."""
    picture: String
    born: String
    height: Float
    country: String
    # movies: [MovieType]
}

type Query {
    getAllActors: [ActorType]
    getActorById(
        id: Int!): ActorType
}